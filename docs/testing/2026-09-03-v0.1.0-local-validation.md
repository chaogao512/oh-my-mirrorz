# oh-my-mirrorz V0.1.0 本地验收记录

- 日期：2026-09-03
- 平台：macOS arm64
- 工具链：Go 1.27.1（`go.mod` 最低语言版本 1.26）
- 结论：本地发布门禁通过；远端 GitHub Actions 状态在推送后另行核对

## 自动化检查

| 检查 | 结果 |
| --- | --- |
| `gofmt` / `git diff --check` | 通过 |
| `go test ./...` | 通过，60 个具名测试 |
| `go test -race ./...` | 通过 |
| `go vet ./...` | 通过 |
| `FuzzValidate` | 通过，5 秒运行超过 100 万次输入 |
| 覆盖率 | 58.1% statements；关键事务与 URL 安全路径有定向测试 |
| 交叉构建 | Darwin/Linux × amd64/arm64 全部通过 |
| 安装脚本语法 | `sh -n` 通过 |
| GitHub Actions YAML 解析 | CI、Release 均通过本地 YAML 解析 |
| 发布包布局 | Darwin arm64 模拟打包、SHA-256、解包与版本执行通过 |

## 安全与行为验证

1. 在 `/private/tmp` 的隔离 HOME/XDG 目录中完成 pip、npm、Homebrew 的 `switch --yes`。
2. 写入后对 PyPI Simple、npm ping、Homebrew API 进行真实 HTTPS 验证，事务成功提交。
3. Homebrew 本机命令确认 `brew.env` 的无引号 `KEY=VALUE` 可被正确读取。
4. 指定事务 ID 完成恢复；新建文件被删除，恢复事务可再次撤销；重复恢复明确返回 no-op。
5. 真实网络 `benchmark` 通过：PyPI 1/1、npm 1/1、Cargo 1/1、Homebrew 1/1、APT 4/4。
6. APT 使用模拟 Debian/Ubuntu 系统根验证 `.list`、DEB822、ports、独立 Debian Security、默认保留 security、保留 PPA/Docker/第三方源。
7. 系统写入通过可替换命令执行器验证仅调用受限 `sudo install/mv`；非 APT 系统路径被拒绝。未在当前 macOS 上修改真实 `/etc` 或执行真实 sudo。
8. 损坏 manifest、缺失快照或摘要不符会阻断历史读取、doctor 与新事务。
9. 凭据扫描未发现真实令牌、私钥、用户配置、快照或缓存进入项目。

## 已知边界

- V0.1.0 不更改 Homebrew Brew/Core Git remote，避免 `brew update` 产生无法由普通文件快照完整恢复的隐藏状态。
- APT 的真实系统写入需要在用户自己的 Debian/Ubuntu 主机上显式传入 `--system`；发布前测试使用隔离系统根和模拟 sudo 执行器。
- 镜像站连通性是动态状态；执行每次真实切换时仍会重新验证协议入口。
